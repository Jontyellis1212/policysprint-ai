{
  "name": "policysprint-ai",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "predev": "node scripts/copy-pdf-worker.mjs",
    "dev": "next dev",
    "build": "next build",
    "start": "node -e \"const { spawn } = require('child_process'); const marker = 'RAILWAY_MARKER_3fcfc04'; const port = process.env.PORT; console.log(marker); console.log('process.env.PORT=', port); if (!port) { console.error('FATAL: process.env.PORT is undefined (Railway should inject this).'); process.exit(1); } console.log('Starting Next on port', port); const child = spawn('./node_modules/.bin/next', ['start', '-H', '0.0.0.0', '-p', String(port)], { stdio: 'inherit' }); child.on('exit', (code, signal) => { console.log('next exited with code=', code, 'signal=', signal); process.exit(code ?? 0); });\"",
    "migrate:deploy": "prisma migrate deploy",
    "lint": "eslint",
    "postinstall": "prisma generate && node scripts/copy-pdf-worker.mjs"
  },
  "dependencies": {
    "@auth/prisma-adapter": "^2.11.1",
    "@prisma/adapter-better-sqlite3": "^7.1.0",
    "@prisma/client": "^6.19.1",
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.5.0",
    "next": "16.0.10",
    "next-auth": "^5.0.0-beta.30",
    "openai": "^6.9.1",
    "pdfjs-dist": "^5.4.530",
    "pdfkit": "^0.17.2",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "resend": "^6.8.0",
    "stripe": "^20.1.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/pdfkit": "^0.17.4",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.0.10",
    "prisma": "^6.19.1",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
